# Voice-Assisstant-Backend — all calling, campaign, history, agent-status APIs
EXPO_PUBLIC_API_URL=http://localhost:8000

# Qualia-Commons-Backend — login / refresh / logout
EXPO_PUBLIC_COMMONS_API_URL=http://localhost:4100

# Set to "true" to enable mock data + skip-login button (offline dev only)
EXPO_PUBLIC_ENABLE_DEV_MOCKS=false
